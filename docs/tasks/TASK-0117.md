---
id: TASK-0117
title: Implement ADR-0016.4 — owner marking, five hosting networks, archiver kind, disclosed backfill
status: done
parent_id: FLDR-0007
created_at: '2026-09-03T17:00:00.000Z'
created_by: fable
updated_at: '2026-09-03T17:00:00.000Z'
type: task
---
Sections A–D of `docs/adr/0016.4-owner-exclusion-and-network-evidence.md`. Found by reading all 42 Browsers-class rows for post 023: 7 were the author (owner exclusion is an exact IP match against a static list and his address is dynamic), 14 were infrastructure on networks missing from `HOSTING_NETWORKS` or an archiver stored as a browser, and about 21 were plausible readers. Deliverables: `POST /api/owner` guarded by `ADMIN_SECRET` that records the caller's daily client id in a new `owner_clients` table; read-model exclusion of those ids in `stats.ts` without mutating rows; five verified hosting ASNs (29802, 64267, 150436, 59711, 25820) with Hurricane Electric, Internet Archive and Black Mesa refused for the reasons in the ADR; `ARCHIVER_NETWORKS` with AS7941 mapped to `preview-or-feed` reason `archiver-asn:7941`; migration 0008 reclassifying only rows on the newly added ASNs, disclosed with the 2026-09-03 boundary; the analytics test suite asserting migration and `networks.ts` stay in sync as it already does for 0006.


2026-09-03 17:30 UTC: done in 991d637 (Codex). `POST /api/owner` with constant-time `ADMIN_SECRET` check, `ON CONFLICT DO NOTHING`, response never echoes the id; stats exclude with `NOT EXISTS (SELECT 1 FROM owner_clients WHERE ...)` on both the window query and the all-time boundary; five hosting ASNs plus `ARCHIVER_NETWORKS`; archiver rule placed after named agents and headless detection, before the bot and browser rules; migration 0008 bounded to `observed_at < '2026-09-04 00:00:00'` and `reader_kind IN ('browser','legacy-browser')`. Verified by Fable: typecheck 0 errors, analytics 37/37, 15 posts valid, 24 built, no banned type assertions. NOT deployed and migration NOT applied; both wait on Goga's push and `wrangler d1 migrations apply blog-analytics --remote`.

2026-09-04 18:30 UTC: migration 0008 APPLIED to remote D1 (8 commands, 22ms). Verified: 38 revisions recorded for 38 distinct observations, from/to breakdown matches the pre-migration export exactly (identical ids, zero differing prior values), 6092 total observations, 0 eligible rows left unrewritten, `owner_clients` created and empty, no DELETE in the migration. Public Browsers for post 023 fell 52 to 37. Still owed: the push and deploy, then one `POST /api/owner` call so the author's own seven views stop counting.
