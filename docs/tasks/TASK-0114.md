---
id: TASK-0114
title: Accept the bare-URI Signature-Agent form so DuckDuckBot's signatures verify; keep the agent token in reader_reason
status: todo
parent_id: FLDR-0007
created_at: '2026-09-03T06:50:00.000Z'
created_by: fable
updated_at: '2026-09-03T06:50:00.000Z'
type: task
---
Found 2026-09-03 while checking Worker health after the ADR-0016.3 deploy. `page_observations` has rows with `signature_status='unverified'`, `reader_reason='malformed-signature-agent'` for `DuckDuckBot/1.1` on `/`. The raw header (tail capture 04:59:33, 05:14:32, 06:23:30) is `Signature-Agent: https://bot.duckduckgo.com`, an unquoted bare URI: neither the sf-string form (`"https://…"`) nor the Dictionary form the current draft specifies, so `parseSignatureAgentEntries` in `packages/analytics/src/webbotauth.ts` returns null. Covered components are `("@method" "@authority" "@path" "signature-agent")`, keyid `5HvpfNA946z6lcCXEIOOpzlN5l0mdZE42jdqimEwPf8`; `https://bot.duckduckgo.com/.well-known/http-message-signatures-directory` serves keys (checked 2026-09-03). AhrefsBot also signs (`Signature-Agent: "https://ahrefs.com"`, quoted form, with a nonce) and should verify already.

Do:
1. Accept a bare URI (`https://` scheme, no whitespace, no `,` `;` `=`) as a third, legacy form; record it as `legacy: true` with a distinct reason string when verification fails (`bare-uri-signature-agent`) so the stats can show the non-conformance as a fact.
2. When a signature is unverified, keep the agent token in `reader_reason` (e.g. `DuckDuckBot; signature:bare-uri-signature-agent`) instead of replacing it; today `readerkind.ts` overwrites the reason and the row loses the agent name.
3. Test with the three captured DuckDuckBot header sets and the AhrefsBot set (values in `packages/blog/drafts/research/ai-fetcher-headers/03-probe-log.md`, section "Worker health").
4. Verify against the live directory in a test that is skipped offline.
Not before the uncommitted `packages/analytics/src` changes from the other session are committed or reverted; do not mix.
