---
id: TASK-0053
title: 'Review artifact: TASK-0048 post 020 fact-check claim ledger'
status: done
parent_id: FLDR-0002
references:
  - url: 'https://gkoreli.com/stats'
    title: Live stats methodology
  - url: 'https://gkoreli.com/privacy'
    title: Live privacy disclosure
  - url: 'https://gkoreli.com/api/stats?range=all&traffic=all'
    title: Live public stats API
  - url: >-
      https://github.com/gkoreli/blog/tree/c85a629d1074db54d5f9e5c171abbd798be85945/packages/analytics
    title: Pinned old analytics implementation
  - url: >-
      https://github.com/gkoreli/blog/tree/f9d65ae9618c00c464c4ed274fc52534f352513b/packages/analytics
    title: Pinned edge analytics implementation
  - url: 'https://developers.cloudflare.com/ai-crawl-control/reference/graphql-api/'
    title: Cloudflare AI Crawl Control GraphQL API
  - url: 'https://developers.cloudflare.com/web-analytics/get-started/'
    title: Cloudflare Web Analytics JavaScript setup
evidence:
  - >-
    Article blockers were applied: owner chronology, aggregate predicates, full
    eligibility excerpt, migration mapping, referrer precision, provider links,
    and public provenance.
created_at: '2026-08-26T16:51:28.905Z'
updated_at: '2026-08-26T16:53:46.328Z'
type: task
---
# TASK-0048 — Post 020 ranked fact-check claim ledger

Read-only audit completed 2026-08-26. Audited the post, research index, TASK-0035, pinned revisions `c85a629d1074db54d5f9e5c171abbd798be85945`, `f9d65ae9618c00c464c4ed274fc52534f352513b`, and `0df8c5936b2cfa6a1d9f77218262aacbd489100c`, ADR-0016/0016.1, migrations/schema, current implementation, live `/stats`, live `/privacy`, live stats API, retired `/api/event`, post 019, TASK-0020, TASK-0040, TASK-0044, and the primary Cloudflare/RFC/EDPB references. No repository files were edited and no formatter, build, typecheck, test, deploy, or broad validation ran.

## Final verdict

**FAIL — publication blocked.** The owned thesis, 2,564-row continuity decision, HMAC/privacy boundary, architecture, and unresolved allocation question are well supported. Publication is blocked by one live prohibited public claim and four article-level factual/code errors. The current draft itself says the owner-exclusion claim must be corrected before publication; as of the audit, both live `/stats` and `/privacy` still say the author's requests are excluded. Correcting that live text also requires updating the article's present-tense owner section so it does not become false at the moment of correction. Do not wait for the 30-day continuation; it is unrelated to these blockers.

## Ranked failures and exact corrections

| Rank | Result | Severity | Evidence state | Exact line / section | Finding | Exact proposed fix |
|---:|---|---|---|---|---|---|
| 1 | FAIL | P0 blocker | Volatile production observation, checked 2026-08-26 | Live `/stats` methodology; live `/privacy` First-party analytics; post lines 290–300 | Both live pages still make the prohibited operational claim: `/stats` says `my own requests are excluded`; `/privacy` says `Public stats exclude my requests.` TASK-0035 found no active `OWNER_IPS` binding and zero owner-marked rows. The repository source has corrected conditional wording, but it is not deployed. | Before publication, deploy and verify the current source wording: rows marked as mine are excluded, but marking depends on server-side configuration and is not proved for all author requests. Alternatively configure `OWNER_IPS`, create and verify owner-marked rows, and retain evidence. |
| 2 | FAIL | P0 blocker after rank 1 is fixed | Volatile-to-historical state transition | Post lines 294–300 and 367 | The section is accurate at the audit instant, but either permitted fix in line 300 makes parts of it stale. If public copy is corrected, line 298 can no longer say the public methodology *says* exclusion, and line 367 can no longer say there is a public owner-exclusion claim. If owner marking is configured, lines 294–300 are also no longer current. | After the production fix, re-observe and rewrite only the state claims. For the copy-correction branch: `On August 26, the active Worker exposed no OWNER_IPS binding and neither era had owner-marked rows. The public methodology said my requests were excluded, so I corrected that wording before publishing; the server-side owner mechanism remains unconfigured or unproved.` At line 367 use `an owner-exclusion mechanism I have not configured or proved`, not `a public ... claim`. Preserve the live uncertainty without publishing a false present tense. |
| 3 | FAIL | P1 factual | Code-inspected stable fact | Post line 128, `One wrong noun crossed every layer` | `Every ... row received the same owner, source, class, and time predicates` is false. `packages/analytics/src/stats.ts` has no `observation_source` predicate; all aggregates include both source eras. The shared predicate is time + owner + optional traffic class + optional path. | Replace `owner, source, class, and time predicates` with `owner, traffic-class, path, and time predicates`. If source is mentioned, say `Every aggregate includes both source eras; observation_source preserves provenance but is not a public filter.` |
| 4 | FAIL | P1 code excerpt | Code-inspected stable fact | Post lines 92–105, eligibility TypeScript | The excerpt is presented as the eligibility policy but omits two real branches. Pinned `f9d65ae` rejects `/stats` **and `/stats/*`**, plus `/api` **and `/api/*`**. The draft rejects only exact `/stats` and `/api/*`. | Use the pinned condition verbatim: `if (path === '/stats' || path.startsWith('/stats/') || path === '/api' || path.startsWith('/api/')) return false`. Keep the rest of the excerpt unchanged or label it explicitly as simplified pseudocode. |
| 5 | FAIL | P1 code excerpt | Code-inspected stable fact | Post lines 218–223, migration SQL | `'observation_source' = 'beacon'` quotes the column as a string literal and the two displayed lines are not a valid excerpt from migration `0002`. The real migration inserts the literal `'beacon'` and old `id` by column position. | Do not label the current mapping as SQL. Prefer a `text` block: `observation_source = beacon` / `source_event_id = page_views.id`; or show the actual `INSERT ... (observation_source, source_event_id) SELECT ... 'beacon', id FROM page_views` fragment. |
| 6 | FAIL | P1 privacy precision | Code-inspected stable fact | Post line 51, old-event description | `preserve document.referrer` overstates stored data. The old beacon sent `document.referrer`, but ingestion reduced it to hostname + path, removed query/fragment, stripped trailing slash, and removed self-referrals. The legacy archive never preserved the full referrer. | Replace with `derive a cleaned referrer hostname/path from document.referrer`. This matches old code and the live privacy disclosure. |
| 7 | FAIL | P2 sourcing/volatility | Primary documented provider capability | Post line 286, `The edge still refuses to count some things` | `Cloudflare's logs and AI Crawl Control can provide a no-code baseline` is plausible but unlinked and over-broad. AI Crawl Control documents path/status/UA filters and estimates; UA filters can be spoofed, and verified detection IDs require Bot Management. It does not prove all non-HTML use or intent. | Link Cloudflare's AI Crawl Control overview and GraphQL API. Narrow to: `Cloudflare request analytics can provide a no-code estimate for successful requests to those resource paths; AI Crawl Control can provide a narrower crawler view. Sampling and identity limits still apply.` |
| 8 | FAIL | P2 provenance | Observed production claim with inaccessible shorthand | Post evidence ledger line 384 | The 2,564-row production claim cites a migration plus plain text `TASK-0020`. The migration proves intended mapping, not that production ran it. TASK-0020 is the observed production evidence but readers cannot follow the shorthand. It also records 0 missing, while the stronger 0-extra/0-mismatch proof comes from TASK-0035. | Hyperlink pinned `docs/tasks/TASK-0020.md` and, once committed, a pinned TASK-0035 ledger; or state `author's read-only production verification` and list the exact checks. Do not let migration source stand in for execution evidence. |
| 9 | FAIL | P2 source entailment | Primary documented | Post evidence ledger line 387 | The Cloudflare overview supports browser-experienced performance but does not by itself show the JavaScript-beacon setup behind `browser-side RUM`. | Add Cloudflare's `/web-analytics/get-started/` link, which explicitly documents the JS snippet/injection, or rename the ledger claim `browser-side performance analytics` and keep `RUM-like` in the body. |
| 10 | FAIL | P2 ledger hygiene | Code-inspected | TASK-0035 line 108 | TASK-0035 says ADR-0016 line 164 still starts `All` at the first edge observation. The current ADR already says first non-owner source-marked observation. This contradiction entry is stale. | Delete the row or mark it resolved with the correcting revision. It is not an article blocker, but the final evidence ledger should not preserve a resolved contradiction as live. |
| 11 | UNVERIFIED | P3 link check | External target blocked to automation | Post line 371 | All internal, GitHub, RFC, Cloudflare, and EDPB targets resolved or were verified from the pinned repository objects. X blocked automated retrieval, so the profile response could not be independently inspected. The handle matches the site's other author links. | Manually click the X link in the final browser pre-publish check. |

## Pass ledger

| ID | Result | Claim class | Exact section / lines | Evidence and boundary |
|---|---|---|---|---|
| P1 | PASS | Dated historical observation | Lines 31–33 | The old public label was `AI Reads`; value 97 appears in post 019 and the final legacy class count. It is a beacon/UA-classification count, and the article immediately rejects the stronger claim. |
| P2 | PASS | Stable engineering fact | Lines 37–66 | Pinned old config ran Worker-first only for `/api/*`; page HTML emitted a later `/api/event` beacon; ingestion classified the beacon UA and wrote `page_views`; old stats counted `visitor_type=2` as `ai_fetches`. The row boundary and non-JS blind spot are stated correctly. |
| P3 | PASS with line-51 correction | Stable engineering fact | Lines 49–51 | A stored old row means the page script ran, a later request arrived, and D1 accepted a row. It does not prove attention. Only the full-referrer wording fails. |
| P4 | PASS | Stable event definition | Lines 80–112 | Pinned `f9d65ae` routes asset fallback through the Worker, applies method/status/path/prefetch/content-type eligibility, and schedules D1 persistence with `waitUntil`. `PageObservation` is correctly narrowed to a recorded eligible HTML GET and explicitly does not prove rendering, reading, or personhood. |
| P5 | PASS except rank 3 | Stable architecture/schema fact | Lines 120–169 | Routing, vocabulary, HMAC identity, source-aware read model, UTC windows, presentation/privacy contract, file ownership, and composition-over-inheritance claims match ADR-0016 and pinned code. Only the nonexistent aggregate `source` predicate is wrong. |
| P6 | PASS | Stable privacy fact | Lines 173–190 | Old ID was first 64 bits of SHA-256 over public UTC date/IP/UA with delimiters; bounded candidate inputs permit offline testing. New code derives a date key with HMAC-SHA-256, signs length-prefixed site/date/IP/UA fields, stores 128 bits, and requires a nonempty secret. Raw IP/UA are transient inputs, not edge-table fields; within-day linkage is pseudonymous, not anonymous or a person ID. |
| P7 | PASS | Dated production snapshot | Lines 203–210 | TASK-0035 production audit supports 2,564 legacy rows from 2026-03-07 through 2026-08-26: 2,120 Browser, 347 Bot, 97 AI-UA; arithmetic reconciles; 39 paths, 32 non-null referrer values, 69 countries. These are beacon events, not page requests. |
| P8 | PASS except rank 5/provenance rank 8 | Stable migration + dated production proof | Lines 218–238 | Migration preserves the source table, maps classes, duplicates the 16-hex daily ID to preserve equality/distinct counts, leaves historical agent names null, and uses partial uniqueness plus `INSERT OR IGNORE` for idempotency. TASK-0035 supports 2,564 source/2,564 unique copies/0 missing/0 extra/0 mismatched. Mixed eras remain explicitly non-comparable. |
| P9 | PASS | Stable trade-off/inference | Lines 240–254 | Old beacon advantages and edge costs match pinned routing and code. User-Agent classes remain sender-provided; D1 writes remain best effort. The bounded inference `server-side is better for this claimed event, not universally` is correctly owned. |
| P10 | PASS | Stable instrument boundary + volatile enabled state | Lines 256–269 | Edge observation and RUM answer different questions. Live raw HTML includes Cloudflare's JS beacon, privacy says it is separate, and first-party D1 does not ingest it. `RUM-like` is calibrated. |
| P11 | PASS except rank 7 | Stable non-HTML boundary | Lines 271–288 | `run_worker_first: true` exposes all named resource requests to Worker routing, while eligibility rejects non-HTML from D1. The article explicitly refuses to call Markdown a page view and does not claim retrieval/use. |
| P12 | PASS at audit cutoff | Dated volatile production observation | Lines 290–300 | TASK-0035's deployment/version inspection and row audit support absence of `OWNER_IPS` and zero owner rows at the audit. The article correctly says code capability is not operational evidence. It must be timestamped and transitioned after the required production correction. |
| P13 | PASS | Engineering inference/decision rules | Lines 304–353 | Event-first naming, schema vocabulary, bounded contexts, method-source preservation, privacy threat modeling, and metric-to-decision rules are reasoned principles grounded in the case and ADR-0016.1, not misrepresented empirical laws. |
| P14 | PASS | Declared future protocol | Lines 355–365 | TASK-0040 defines the separate 30-day UTC comparator/window, costs/failures, HTML/RUM/non-HTML separation, and limits. TASK-0044 is a frozen preregistered HTML-vs-Markdown benchmark and explicitly reports no model runs. Neither is presented as a result. |
| P15 | PASS except rank 2 | Inference/live question | Lines 367–371 | The allocation question is explicitly unanswerable from the dashboard and does not claim a completed outcome. It is the correct field-note boundary. Only the owner-claim noun must follow the production state change. |
| P16 | PASS | Link/date audit | Lines 375–388 | Pinned old/new SHAs and files exist; 0df ADR target exists; internal post target resolves; RFC 2104, Cloudflare routing, Cloudflare Web Analytics, and EDPB targets resolve and entail the bounded claims. Dates align: cutover and production checks Aug 26, Cloudflare Web Analytics updated Apr 16, 2026. |
| P17 | PASS | Prohibited-claim audit | Whole post | Uses of Human/Visitor/AI Read/read/person/anonymous are rejected labels, not asserted identities. No claim of indexing, citation, training, model use, verified provider identity, cross-day uniqueness, exact traffic, perfectly comparable eras, RUM ingestion into D1, or 30-day outcome survived. |
| P18 | PASS | Research-index contract | `00-research-index.md` | Post 019 owns llms.txt/GEO; post 020 owns the analytics rebuild, 2,564-row continuity, principles, architecture, and unresolved allocation. The 30-day continuation remains separate. Stable claims 1–10 otherwise reconcile with code/ADRs/production evidence. |

## Publication gate

Publication may proceed after all P0/P1 rows are corrected and re-observed in one coherent cutoff: live owner wording, owner-section tense/state, aggregate predicate wording, exact eligibility excerpt, SQL/pseudocode block, and old-referrer wording. P2 fixes are strongly recommended for the evidence-led release because they make provider and production provenance followable, but they do not require waiting for future data. After fixes, recheck the live `/stats` and `/privacy` wording and manually click the X target; do not mix new live totals into the article because the draft correctly relies only on the frozen 2,564-row source archive.
