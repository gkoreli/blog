# ADR-0016.4: Owner Exclusion and Network Evidence After the First Public Article

## Status

Accepted — 2026-09-03. Sections A to D were implemented in commit `991d637` (owner endpoint and `owner_clients` table, read-model exclusion, hosting networks, `ARCHIVER_NETWORKS`, migration 0008; analytics suite 29 to 37 tests). The old undeployed/unapplied status is superseded: migration 0008 ran September 4 at 18:30:09 UTC and recorded 38 revisions, as preserved in [the production follow-up](../../packages/blog/drafts/research/readers-vs-bots/12-production-followup-2026-09-05.md). The [September 6 parser/provenance release](../../packages/blog/drafts/research/edge-vs-rum/04-release-verification.md) includes the owner/network code, but a controlled owner mark and resulting public exclusion remain unverified in these receipts. Section E stays open as TASK-0118. This extends ADR-0016.2 and ADR-0016.3; the original trigger was post 023's September 3 traffic.

**Interpretation correction, September 7:** the original 42-row assessment below is a historical classification exercise. Its 21 plausible reader-like observations are requests, not verified people. Neither that split nor the later rule reduction establishes audience accuracy or a lower bound on readership. [Claims ledger](../../packages/blog/drafts/research/readers-vs-bots/16-claims-and-work-status-2026-09-06.md).

## Context

The public stats page showed **42 views** in the Browsers class for `/which-ai-fetchers-send-which-headers`. Goga asked whether those were real readers. The original manual review compared the rows with RIPEstat registry information and assigned these groups:

| Manual assessment | Rows | Evidence recorded at the time |
|---|---:|---|
| The author | 7 | AS62887 WhiteSky, one daily client id, 05:06 to 06:37 UTC, including the click on his own X link |
| Infrastructure-associated requests | 14 | Internet Archive ×2; Hivelocity ×4 (two exact-duplicate timestamps, referrer `hnagg.com`); Sprious/Rayobyte ×1 (referrer `hacker-news.firebaseio.com`); Byteplus ×2; HZ Hosting ×1; IT7 Networks ×1; Black Mesa ×1; Hurricane Electric ×2. These associations do not independently prove each request's purpose |
| Plausible reader-like requests | 21 | Consumer and university networks: Charter, Comcast, Cox, Windstream, TELUS, Vodafone ×2, Free SAS, Virgin Media, RWTH Aachen, Atria Convergence, China Mobile, China Telecom ×2, SK Broadband, HGC, NETV, Hulum Almustakbal, Akamai mobile, and two small ASNs. Eleven carried a `news.ycombinator.com` referrer; neither network nor referrer verifies a person |

This review identified owner-exclusion and classification problems to investigate. It did not establish that half of the Browser total was wrong or that 21 people read the page. The following implementation findings motivated the repair.

**1. Owner exclusion originally used an exact IP match against a static list.** `extractRequestMetadata(request, env.OWNER_IPS)` set `isOwner` with `owners.includes(ip)`. A changed home address could silently stop matching. The September 3 investigation attributed AS62887 observations to the author and found the static exclusion had missed them. The earlier note did not preserve a complete owner message for that finding, so its fragment quotation is retired.

**2. The hosting network list is missing providers that demonstrably carried automation to this site.** `HOSTING_NETWORKS` has 23 entries. Five networks in the 42 rows are hosting or proxy businesses and are absent: Hivelocity (AS29802), Sprious (AS64267, the Rayobyte proxy business), Byteplus (AS150436), HZ Hosting (AS59711) and IT7 Networks (AS25820).

**3. An archiver is stored as a browser.** Internet Archive (AS7941) fetched the page twice within four seconds of the Hacker News submission, using a browser User-Agent, so `readerkind.ts` recorded `browser`. Confirmed as the Wayback Machine: the snapshot at `web.archive.org/web/20260903061604/` carries the timestamp 2026-09-03 06:16:04 UTC, matching the second row to the second. Save Page Now is not a reader.

Two known problems recurred and are recorded here rather than fixed:

- **Cloudflare's `as_org` names the IP block's registered organisation, not the announcing network.** Two rows say `Chiron Software LLC` where the registry says Cox Communications, and one says `AviationAI` where the registry says Comcast. The stats page displays `as_org`, so the public page shows names that suggest hosting where the network is a consumer ISP. Post 023 documents the same mismatch for nine of sixteen Grok exits.
- **Duplicate requests at identical timestamps.** The Hivelocity client produced two rows at exactly 06:20:00 and two at exactly 07:25:00. Each is a real request; nothing is deduplicated.

## Decision

### A. Owner exclusion is marked by the owner, not inferred from an address

Add an authenticated endpoint that records the caller's current daily client id as the owner's for that UTC day, and exclude those ids in the read model.

- `POST /api/owner` with the existing `ADMIN_SECRET` as a bearer token. It computes the caller's `daily_client_id` exactly as ingestion does and inserts it into a new `owner_clients(daily_client_id, utc_date, marked_at)` table.
- The stats read model excludes rows whose `daily_client_id` appears in `owner_clients`, in addition to the existing `is_owner = 0` filter.
- Rows are **not** mutated. `is_owner` keeps recording what ingestion knew at write time; the exclusion is a read-model fact with its own timestamped audit row. Tenet 4 of ADR-0016.3, never lose history, is why.
- `OWNER_IPS` stays. It is still correct when the address is stable and costs nothing.

Limits, stated on the stats page: the marking covers one browser, one address and one UTC day. A changed address mid-day needs a second call. This narrows the error; it does not eliminate it.

### B. Hosting networks: five additions, three refusals

Added, each verified against Team Cymru and RIPEstat on 2026-09-03. The ADR-0016.2 test applies: a network is added only when it is a hosting or data-centre business, never when it sells consumer VPN exits that a reader might sit behind. Rayobyte sells residential proxies as a separate product; AS64267 is its data-centre ASN, which is what this rule convicts.

| ASN | Registry holder | Evidence on this site |
|---|---|---|
| 29802 | HIVELOCITY, Inc. | four requests, two pairs at identical timestamps, referrer `hnagg.com` |
| 64267 | Sprious LLC (Rayobyte data-centre proxies) | referrer `hacker-news.firebaseio.com`, an unusual source declaration that does not by itself prove automation. The original review also used [Qurium Media Foundation's Rayobyte investigation](https://www.qurium.org/weaponizing-proxy-and-vpn-providers/rayobyte/) as network evidence |
| 150436 | Byteplus Pte. Ltd. (ByteDance cloud) | two requests in the same second from two client ids |
| 59711 | HZ-EU-AS HZ Hosting Ltd | browser-shaped request, hosting-only network |
| 25820 | IT7NET, IT7 Networks Inc | browser-shaped request, dedicated-server business |

Three more were added on 2026-09-04, from the +36h read of the same page:

| ASN | Registry holder | Evidence on this site |
|---|---|---|
| 213230 | HETZNER-CLOUD2-AS, Hetzner Online GmbH | a second Hetzner ASN; AS24940 was already listed and this one was not |
| 62610 | ZEN-DPS, Zenlayer Inc | edge-cloud provider, browser-shaped request |
| 139341 | ACE-AS-AP, Aceville Pte Ltd | two requests at the same second from two client ids geolocated in Thailand and Mexico |

Refused, with the reason:

- **AS6939 Hurricane Electric.** Transit plus a free IPv6 tunnel broker that real people use. Convicting it would convict readers, which is the same rule that kept M247 and Datacamp out in ADR-0016.2.
- **AS7941 Internet Archive.** Not hosting. Handled in section C.
- **AS46997 Black Mesa Corporation.** One request, unclear business. Not enough evidence.

### C. Archivers are named as archivers

Introduce `ARCHIVER_NETWORKS`, initially `AS7941 → internet-archive`. A request from an archiver network that carries no vendor agent token is stored as `preview-or-feed` with reason `archiver-asn:7941`, the group that already holds link-preview fetchers, feed readers and monitors. A dedicated `archiver` reader kind is deliberately not added: the public partition should change only when the volume justifies a new column, and two requests do not.

### D. History is corrected by a dated, disclosed migration

Migration `0008` reclassifies only the rows whose ASN is in the newly added hosting or archiver sets. This follows the precedent of migrations 0006 and 0007: a backfill is allowed when the method changed, when it is recorded in a migration file, and when the ADR names the boundary.

**The boundary is the migration, not a calendar date.** The first implementation bounded the rewrite at `2026-09-04 00:00:00`, taking the ADR's date literally. That was wrong: the corrected code was not deployed on the ADR's date, so every row written between the ADR and the deploy came from the old classifier and would have been left behind a stale cutoff. The +36h read found exactly such rows. Narrowness comes from the ASN and `reader_kind` filters instead, and the migration runs once.

**Nothing is lost, per row.** Migrations 0006 and 0007 rewrote `reader_kind` in place, so the previous verdict was recoverable only by reading their SQL, never for a given observation. ADR-0016.3 tenet 4 asks for more than that. Migration 0008 therefore creates `reader_kind_revisions(observation_id, migration, from_kind, from_reason, to_kind, to_reason, revised_at)` and records every rewrite before it happens; future reclassifications append to the same table. `page_views` remains the lossless archive of the original beacon events, and the 38 rows this migration rewrites were also exported out of band before it ran.

### E. Not decided here

- **Whether the stats page should show the registry holder instead of Cloudflare's `as_org`.** The registry holder is the more accurate name, but resolving it needs an external lookup and a stored mapping. Recorded as a task, not decided.
- **Whether additional client-side confirmation is useful.** The 42-row review did not prove that all useful server evidence was exhausted or that a script would verify a person. Inspect existing logs and execute controlled collection trials in TASK-0120 before selecting new telemetry. TASK-0104 has no universal ratio threshold that automatically justifies instrumentation.

## Consequences

- The affected requests can move out of Browsers under the changed rules; the actual report effect requires its own dated measurement. The original prediction of 42 becoming about 21 was not a measured post-release reader count. Browsers can include automation and exclude legitimate access, so no readership bound follows.
- The composition table gains rows in Automation and Preview or feed.
- Post 023's `08-metrics.md` preserves the row-level assessment. Its interpretation and the article's semantic corrections remain part of TASK-0107; manual plausibility labels must not become verified people.
- The next monthly re-probe (TASK-0116) and the external calibration (TASK-0104) both compare against the corrected series, and the correction date is disclosed alongside them.
