/**
 * Canvas entry — neural canvas hydration for pages with a sec-canvas-banner
 * (homepage + section archives). Kept separate from main.js per the
 * per-page bundle isolation pattern (ADR-0005).
 */
import './components/neural-canvas.js';
