# Desktop takeover and evidence review

September 9 UTC / September 8 PDT, 2026. Root desktop task: `01a08387-f5af-74e2-9141-095760c9aba3`. Blog baseline at takeover: `c4c924fa9005a1e30e99862ab5e871d8fa6b257c`, branch `main`, clean working tree. Read the referenced ChatGPT conversation, the article worklist/readiness assessment, source ledger, experiment plan, and #06 publication module before continuing. No delegated agents were used.

## Article passport

- **Form:** OSS Radar project deep dive.
- **Living center:** a reusable evaluator reduces experiment work only if the records needed to question its results survive the adapter and export path.
- **Owned workload:** Goga wants to observe and inspect citations of engineering writing, then relate bounded answer observations to his site's analytics.
- **Role:** reader-growth, with the blog explaining why this capture problem matters now.
- **Protected evidence:** the built-in cache retains the parsed fixture. Do not broaden an adapter-to-summary omission into universal evidence destruction. Local retention does not establish live response shape, source support, organic discovery, or causal reliance.
- **Author relationship:** Goga supplies the workload and editorial direction; Codex executes this recorded continuation. Do not imply manual human operation of these tests.
- **Decision:** try the existing runner with a bounded capture provider; use a direct runner if the integration replaces most of the runner's useful work.
- **Prior/next:** first-party analytics provides the request-side context. The planned engineering continuation needs new operated citation-capture evidence. The larger 72-call study remains independent.

## New evidence

[The capture continuation](repro/capture/README.md) exercised ten synthetic cases and eleven HTTP attempts with the installed 0.122.2 package. Every complete received response and attempt record survived the summary, library JSON export, and a fresh CLI process reopening the local database. Five answer assertions passed and five cases deliberately failed. A successful local trace preserved the explicit evaluation/test/trace/target-span join. The original fixture findings remain intact.

The continuation uses the original lock with optional platform dependencies included. An intermediate exporter attempt reported missing `@libsql/darwin-arm64` after the earlier installation omitted optional packages; reinstalling from the same lock with platform dependencies resolved it. Final accepted files and script hashes supersede those intermediate runs. The probe verifies transmitted request text against the saved input and checks both input and response hashes.

| Claim | Product says | Code/test | Evidence state | Inference | Reader consequence |
|---|---|---|---|---|---|
| Raw data and metadata can survive the result pipeline | The response contract exposes both | Ten controlled cases, three result surfaces | Reproduced | Limited to these non-streaming variants and exports | The extension can carry evidence without a fork |
| Failure records need explicit capture | No universal archive promise | Malformed JSON, 429, error envelope, missing answer, truncation | Reproduced | This adapter retains them; not an audit of all built-in error paths | Count failures and missing fields separately from no-citation answers |
| A retry can preserve its failed first attempt | Provider metadata is extensible | One explicitly permitted 429 retry | Reproduced | Retry records are our code, not separate Promptfoo spans | The final successful answer need not hide an earlier attempt |
| Local traces can join to captured attempts | Per-test roots and target spans shipped | One success, exact IDs through both exporters | Reproduced | No remote retrieval history or human-trigger evidence | Tracing adds useful local execution context |
| Full workload adoption remains conditional | No live citation-archive promise | Live provider and bill reconciliation not run | Open, not an evidence state | Local tests pass a prerequisite | Keep the larger live study open |

## Source audit

Retrieved all 34 unique external links in the takeover manuscript: pinned blog artifacts were verified against their exact commits; Promptfoo code was fetched at its baseline SHA; other public pages were retrieved directly. Every link resolved. Read the relevant implementation blocks, original release entries, issue #9968, standards sections, papers' definitions, and first-party product announcements. A successful HTTP response checks accessibility only; it does not itself verify the associated claim.

The source review confirms the two August release tracks, the December 2025 scanner introduction, the March 9 OpenAI announcement, and the continued distinction between stated Frontier plans and inspected shipped code. Issue #9968 remains open at this review. Code inspection confirms the cache-aware OpenRouter transport and its normalized return fields; `raw` and citation metadata are not returned there. The new local trace result is checked against the installed trace path and pinned evaluator-tracing source.

No publication-wide token count is claimed. The prior work ran in a separate ChatGPT conversation and its complete usage records are unavailable here; this desktop task's totals would not describe the whole article.

## Live access

No OpenRouter, Perplexity, or OpenAI API key was present in the task environment. The repository's local environment file contains only newsletter/verification credentials; their values were not printed or reused. Asked the author for an existing API configuration location. Consumer subscriptions are not assumed to provide API billing. No live call, billing action, production D1 query, social post, or upstream message has occurred in this continuation.

The minimum remaining live check is one directed answer, captured as an actual provider response, replayed through the same adapter/export route, and reviewed against its source. A directed article URL tests access and summarization; it is excluded from the proposed organic-discovery cohort. Freeze the exact question and rubric before that call. Do not fabricate a provider payload from a browser answer and describe it as an API capture.
