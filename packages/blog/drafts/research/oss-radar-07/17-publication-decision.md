# Publish the tested Promptfoo findings

September 10, 2026 PDT / September 11 UTC. Publication preparation follows Goga's explicit instruction, preserved as prompt thirteen in the [prompt record](../../../prompts/oss-radar-07-promptfoo.prompts.md).

**Decision: publish the controlled adapter, capture, export, and local-trace findings.** These experiments support a bounded trial recommendation. They do not establish live citation preservation, source-support accuracy, organic discovery, or production adoption. The article already states these limits in its opening, experiment sections, and decision.

The earlier [candidate review](11-publication-candidate-review.md) made a real answer and source review a release gate for the broader workload verdict. We are narrowing that verdict to the tests completed and publishing them now. The [one-answer protocol](12-directed-answer-preflight.md) remains unchanged and unrun as follow-up work. The proposed 72-call study and engineering continuation remain separate. No successful model answer or source-review score is claimed.

## Browser access and the unfinished example

The native browser tool returned `Sky Computer Use native pipe startup failed`. After resetting the session, inventory still returned no apps or browsers and the same native-pipe error. No question reached Perplexity, no answer was collected, and no import evaluation ran. This is an access failure before the experiment, not an inaccessible-source or no-citation model result. We did not buy API credits, substitute a fabricated payload, or retry the question elsewhere for a favorable answer.

## Release scope

- Move the reviewed immersive candidate into `posts/026-oss-radar-07-promptfoo.ts` and set its publication date to September 10, the author's local date.
- Preserve the existing experimental denominators: three original evaluations/two local requests, then ten cases/eleven local attempts. All model payloads were synthetic.
- Refresh material release and issue state in [the source check](16-publication-source-check.md). Retain 0.122.2 as the tested baseline and disclose the newer release without pretending its behavior was rerun.
- Keep the research directory, runnable probes, recorded exports, dated source cards, glossary, and thirteen exact prompts available to readers. No token total is published because the full research-session usage is unavailable.
- Run current type and production-build checks in an isolated checkout. Reuse the [completed desktop/mobile canvas review](15-mobile-depth-and-motion-review.md) for the unchanged presentation; do not describe it as a fresh browser review.
- Commit and push directly to `main`, then verify the deployment and served article, Markdown, prompts, discovery metadata, and assets. Record the result in the release receipt.

The publication request authorizes the blog release. X/HN copy will be prepared separately; no social submission is part of this release.
