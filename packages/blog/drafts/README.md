# Drafts

Committed but unpublished work lives here.

The build pipeline only discovers publishable posts from `packages/blog/posts`.
Files in this directory may be pushed to GitHub, but they are not included in
the generated site, RSS feed, sitemap, `llms.txt`, or `.md` endpoints.

Move a draft into `packages/blog/posts` when it is ready to publish.
